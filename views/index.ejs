<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div class="newbtn" id="newbtn">+ New</div>
    <div class="task-win" id="task-win">
        <textarea class="newtask" id="newtask"></textarea>
        <div class="controls">
            <button class="save" id="save">Save</button>
            <button class="cancel" id="cancel">Cancel</button>
        </div>
    </div>
    <div class="container">
        <header>
            <div style="flex-grow: 1; display: flex; justify-content: center; height: fit-content;">
            <button class="todo tab" id="todo">To-Do
                <div class="badge todo-count" id="todo-count">
                </div>
            </button>
            <button class="done tab" id="done">Done
                <div class="badge done-count" id="done-count" style="background-color: blue;">
                </div>
            </button>
        </div>
        <div style="width: fit-content">
        <form action="/logout">
        <button class="logout" id="logout">Log Out</button>
    </form>
    </div>
        </header>
    </div>
    <div class="container">
        <div class="hero">
            <div class="todo-tab" id="todo-tab">
                <% for (task of userData){%>
                    <%if(task.isDone == false){%>
                <div class="task">
                    <div class="taskcontent"><%=task.task%></div>
                    <button class="done-btn" onClick="doneTask('<%=task.id%>', event)">Done</button>
                </div>
                <% } %>
                <% } %>
                <!-- Hi -->
            </div>
            <div class="done-tab" id="done-tab" style="display: none;">
                <% for (task of userData){%>
                    <%if(task.isDone == true){%>
                <div class="task">
                    <div class="taskcontent"><%=task.task%></div>
                    <button class="undo" onClick="undoTask('<%=task.id%>', event)">Undo</button>
                </div>
                <% } %>
                <% } %>
                <!-- hello -->
            </div>
        </div>
    </div>
    <div class="container">
        <footer>

        </footer>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.8.4/axios.min.js" integrity="sha512-2A1+/TAny5loNGk3RBbk11FwoKXYOMfAK6R7r4CpQH7Luz4pezqEGcfphoNzB7SM4dixUoJsKkBsB6kg+dNE2g==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <script src="script.js"></script>
</body>
</html>